# Placeholder folder for optional dedicated VPA Application path.
# Active VPA manifests live next to workloads:
#   k8s/swagger-app/vpa-*.yaml
#   k8s/app-deployment/vpa-nginx-demo.yaml
# Do not apply this file; Argo ignores unknown kinds only if present — keep README only.
